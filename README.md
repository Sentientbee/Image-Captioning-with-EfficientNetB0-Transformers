# Image-Captioning-with-EfficientNetB0-Transformers
A deep learning project that generates natural language captions for images using a CNN–Transformer architecture.  EfficientNet is used as the visual encoder while a Transformer decoder generates descriptive captions.  Includes a simple GUI application for real-time caption generation from uploaded images.
